<%= form_for(@tipo_pokemon) do |f| %>
  <% if @tipo_pokemon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_pokemon.errors.count, "error") %> prohibited this tipo_pokemon from being saved:</h2>

      <ul>
      <% @tipo_pokemon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_id %><br>
    <%= f.text_field :tipo_id %>
  </div>
  <div class="field">
    <%= f.label :pokemon_id %><br>
    <%= f.text_field :pokemon_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
